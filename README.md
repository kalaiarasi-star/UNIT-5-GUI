This project is a simple graphical timer application developed using Java Swing and multithreading. It demonstrates how threads can be used to perform background tasks while keeping the user interface responsive.

The application displays a timer that counts seconds and provides controls to start, stop, and reset the timer. A separate thread is used to update the timer every second without freezing the GUI.
